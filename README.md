# ConvolutionTestApp

Test application to compare performance of a convolution in the time and frequency domains.

Manually-sorted results are shown in test_results.xlsx, while the test app itself generates a raw CSV file with the times for each test run.
